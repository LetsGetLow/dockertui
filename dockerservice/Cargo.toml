[package]
name = "dockerservice"
version = "0.1.0"
edition = "2024"

[features]
bollard-service = ["bollard"]

[dependencies]
async-trait = { workspace = true}
bollard = { version = "^0.21", optional = true, features = ["chrono"] }
